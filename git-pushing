...or create a new repository on the command line
echo "# programming-reminders" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin git@github.com:tomm02/repository_name.git
git push -u origin main

...or push an existing repository from the command line
git remote add origin git@github.com:tomm02/repository_name.git
git branch -M main
git push -u origin main

Note for pushing to a cloned repo
When you git clone from github the .git/config file may contain
a https link to your repo. You need to change to a ssh equivalent
So in .git/config
change
https://github.com/github_usernam/repo_name.git
to
git@github.com:github_username/repo_name.git
